body {
    background-color: #2e003e !important; /* Dark glossy shade of purple */
    margin: 0;
    padding: 0;
    font-family: 'Roboto', Arial, sans-serif;
    color: white;
}

header {
    background-color: rgba(0, 0, 0, 0.8);
    padding: 10px;
    text-align: center;
    position: relative;
}

nav ul {
    list-style-type: none;
    padding: 0;
    display: flex;
    justify-content: center;
}

nav ul li {
    margin-right: 10px;
}

nav ul li a {
    color: white;
    text-decoration: none;
    padding: 5px 10px;
    border-radius: 5px;
    transition: background-color 0.3s ease;
}

nav ul li a:hover {
    background-color: rgba(255, 255, 255, 0.3);
}

.cart-icon {
    position: absolute;
    top: 10px;
    right: 10px;
    cursor: pointer;
}

main, section {
    padding: 20px;
    background-color: rgba(255, 255, 255, 0.8);
    margin: 10px;
    border-radius: 8px;
    color: black; /* Ensures text is readable on the white background of sections */
}

h1, h2 {
    text-align: center;
}

#contact a {
    color: #ffcc00; 
    text-decoration: none;
}

#contact a:hover {
    text-decoration: underline;
}

.product img {
    max-width: 100%;
    height: auto;
    border-radius: 8px;
}

.add-to-cart {
    background-color: #2e003e; /* Dark glossy shade of purple */
    color: white;
    border: none;
    padding: 10px;
    cursor: pointer;
    border-radius: 5px;
}

.add-to-cart:hover {
    background-color: #4b007d; /* Slightly lighter shade of purple */
}

.cart-sidebar {
    height: 100%;
    width: 0;
    position: fixed;
    z-index: 1;
    top: 0;
    right: 0;
    background-color: #111;
    overflow-x: hidden;
    transition: 0.5s;
    padding-top: 60px;
    color: white;
}

.cart-sidebar .closebtn {
    position: absolute;
    top: 20px;
    right: 25px;
    font-size: 36px;
}

.cart-sidebar-content {
    padding: 20px;
}

.cart-sidebar-content h2 {
    margin-top: 0;
}

.cart-sidebar-content p {
    margin: 5px 0;
}

#cart-items {
    margin-bottom: 20px;
}
